const initialState = {};

const sessions = (state = initialState, actions) => {
    const { type } = actions;
    switch (type) {
        default:
            return state;
    };
};

export default sessions;