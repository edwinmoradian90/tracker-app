const initialState = {};

const trackers = (state = initialState, actions) => {
    const { type } = actions;
    switch (type) {
        default:
            return state;
    };
};

export default trackers;
