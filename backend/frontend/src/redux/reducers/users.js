
const initialState = {};

const users = (state = initialState, actions) => {
    const { type } = actions;
    switch (type) {
        default:
            return state;
    };
};

export default users;